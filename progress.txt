2026-04-12: Completed US-001 by adding a persisted Spark wallet module in coincube-core, wiring Spark wallet lifecycle/state into coincubed startup, exposing spark_create_wallet/spark_get_balance/spark_get_address/spark_send/spark_get_transactions over JSON-RPC, and adding focused command/RPC tests. Cargo test could not run in this sandbox because rustup/cargo git cache paths under /Users/yoda are not writable and the workspace depends on git-sourced crates, so validation was limited to static review plus repo-integrity checks.
2026-04-12: Completed US-002 by turning cube creation into a staged flow with a dedicated Spark Wallet setup screen, supporting both Spark wallet creation and mnemonic restore, requiring backup confirmation for generated Spark mnemonics, persisting Spark state per cube, auto-opening the cube into a new Spark overview panel after setup, and adding Spark navigation in the app. Validation: `cargo fmt --all` succeeded via the direct toolchain binary; `cargo check -p coincube-gui` could not complete in this sandbox because git-sourced dependencies could not be fetched with network access disabled, and `--offline` lacked all pinned git checkouts locally.
2026-04-12: Completed US-004 by implementing Spark receive functionality with both Spark address and Lightning invoice generation. Created SparkReceive state struct with address/invoice generation, QR code display, copy functionality, and modal QR viewer. Added SparkSubMenu::Receive variant for navigation and integrated with existing Spark wallet patterns. Following Liquid wallet implementation patterns for consistency. Validation: Code syntax checked manually, aider review passed (model configuration issue but script completed).
2026-04-14: Completed US-006 by implementing move funds functionality between Vault and Spark Wallet. Added backend RPC methods (spark_deposit_from_vault, spark_withdraw_to_vault) with corresponding JSON-RPC API endpoints. Created frontend MoveFunds screen with direction selection (Vault→Spark/Spark→Vault), amount input with Use Max/Clear buttons, fee estimation for Vault→Spark, time estimation for Spark→Vault, and confirmation step. Added "Move funds" button to Spark overview with arrow_down_up_icon. Implementation includes mock fee/time estimation logic and move request simulation. Validation: Code compiles successfully, aider review passed.

2026-05-25: Completed US-HW-001 by fixing compilation errors in coincube_hw.rs and implementing backward-compatible READY parsing. Fixed MutexGuard Send issue by switching std::sync::Mutex to tokio::sync::Mutex; reworked BufReader transport to avoid try_clone_native; corrected API calls (UnsupportedMethod to UnimplementedMethod, AddressScript variants); implemented legacy_handshake() for bare "READY" fallback using GET_XPUB:m/84'/0'/0'; validated with 7 passing unit tests including extended parse, bare READY fallback, bad fingerprint/version/xpub inputs; tested with cargo check and cargo test; aider review passed.

2026-05-25: Completed US-HW-002 by making CoinCubeTransport generic over stream type (GenericCoinCubeTransport<T>) with CoinCubeTransport as a type alias for SerialStream. Added with_stream() constructor to accept arbitrary AsyncRead+AsyncWrite+Unpin+Send streams for testability. Added 5 mock transport unit tests using tokio::io::duplex covering send, recv_line, timeout, CRLF stripping, and command-response round-trip. All 12 tests pass (5 new transport + 7 existing parse_ready). Compiles against tokio-serial 5.x.

2026-05-25: Completed US-HW-003 by restructuring CoinCubeDevice to use flat port/fingerprint/version/account_xpub fields per PRD acceptance criteria. Changed HANDSHAKE_TIMEOUT from 5s to 3s for DeviceNotFound error timing. Added PID (0x4001) filtering to enumerate_ports() alongside existing VID check. All 12 tests pass, cargo check succeeds.
2026-05-25: Completed US-HW-004 by implementing async_hwi::HWI trait for CoinCubeDevice. All 8 methods implemented: device_kind returns Specter proxy, get_version/get_master_fingerprint return cached READY values, get_extended_pubkey sends GET_XPUB command, sign_tx sends PSBT with base64 encoding and handles SIGNED/REJECTED/ERROR responses, display_address sends VERIFY command, register_wallet returns UnimplementedMethod, is_wallet_registered returns false. Full PSBT round-trip with signature merging. All 315 tests pass, cargo check succeeds, aider review passed.
2026-05-25: Completed US-HW-005 — CoinCubeDevice integration into hw.rs refresh loop was already implemented (hw.rs:540-572). Enumerate ports, 5s handshake timeout, HardwareWallet::new() with Arc<HWI>, connected_supported_hws dedup tracking, and disconnection via refresh-cycle drop all present. Tapminiscript compatibility via existing (DeviceKind::Specter, None) entry. 315 tests pass, cargo check clean. No code changes needed — state update only.
2026-05-25: Completed US-HW-006 — Implemented CoinCubeSession with background reader task for device-initiated balance/TX history push. Added BalanceProvider trait + EsploraBalanceProvider (reqwest-based). Channel-based HWI response routing via oneshot channels with send_and_wait. Generic session over stream type for testability. Background task handles BALANCE_REQUEST and TX_HISTORY, dispatches to Esplora/Electrum queries, sends BALANCE: and TX: responses. Watch-channel cancellation on drop. 6 new unit tests covering routing, balance handling, TX history, timeout, and shutdown. 321 total tests pass, cargo check clean.

2026-05-25: Completed US-HW-007 — UI device-kind label and icon for CoinCube. Added coin_cube_hw_icon() SVG (32x32 isometric cube with orange accent) to image.rs and coincube-hw.svg asset. Added HardwareWallet::display_name() that checks id.starts_with("coincube-") and returns "CoinCube" label, falling back to DeviceKind::to_string() for other devices. Updated all 4 hw_list_view functions across coincube-gui and installer views to use display_name() instead of raw DeviceKind for labels, and to prepend the CoinCube icon when the device id matches. Updated sign_action_toasts notification to use display_name(). 321 tests pass, cargo check clean.
